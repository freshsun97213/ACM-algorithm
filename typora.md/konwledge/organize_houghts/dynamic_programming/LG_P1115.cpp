#include <climits>
#include <iostream>

#define i64 long long

using namespace std;
const int N = 1e6 + 10;
i64 dp[N], a[N];

//

int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }

  return 0;
}
